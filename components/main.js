import styled from 'styled-components'

const MainStyled = styled.main`
  grid-area: main;
  color: var(--white-1);

`

function Main() {
  return (
    <MainStyled>
      Main
    </MainStyled>
    
  )
}

export default Main
